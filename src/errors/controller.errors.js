const ROUTE_DOES_NOT_EXIST = new Error("Router doesn't exist!");

const PATH_DOES_NOT_EXIST = new Error("Path doesn't exist");

const IMPROPER_PATH = new Error("Improper path");

module.exports = { ROUTE_DOES_NOT_EXIST, PATH_DOES_NOT_EXIST, IMPROPER_PATH };
